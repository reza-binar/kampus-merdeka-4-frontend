/* The second way */
const calculateBallLongVersion = (radius) => {
  return (4 / 3) * Math.PI * radius ** 3;
};

const arham = "Arham";
const randomNumber = 10;

module.exports = {
  calculateBallLongVersion,
  arham,
  randomNumber,
};
